# Airbnb experiences page clone using react

Just a mockup to practice react where the only real place it is taking advantage of the library is in the card components which are generated from a "database".
